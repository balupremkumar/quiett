# STATE — voice-dictation

Vault: [[HOME]] | [[PORTFOLIO|Portfolio]] | [[projects/active/voice-dictation/BACKLOG|Backlog]]

Seeded 2026-07-03 from the workflow review; verify against reality on first resume, then keep current via the Stop hook.

## Current state
Hold-to-talk dictation: Ctrl+Alt records, whisper.cpp server (large-v3-turbo-q5_0, Vulkan build under third_party/whisper.cpp) transcribes, preview panel, clipboard injection.
Read-aloud: Ctrl+Shift+S speaks the selection in the cloned voice (qwentts.cpp, Vulkan), chunked for a steady pace, speed 0.7x-1.5x from the tray.
Scope as of 2026-07-25: dictation in, read-aloud out. Nothing else. No LLM runs in the app.
REMOVED 2026-07-25 (de-bloat ruling): TaskFlow capture, agent command mode, Local FitnessPal divert. Deleted taskflow.py, agent.py, llm_client.py, reformat.py, fitness.py, lmstudio_boot.py. Vibe mode had gone 2026-07-01.
Voice profile / cloning work started 2026-07-02: recording samples feature added, profile build in progress.
SHIPPED 2026-08-22 (commits 5b85fec + a9ff082, pushed): insert reliability overhaul per PASTE_UX_PLAN.md. inject_text() no longer reports success on SendInput acceptance alone; the clipboard is only restored on a CONFIRMED insert, so a dictation is never destroyed. New stash.py (one parked dictation + tray dot), targetprobe.py (Win32 caret then UIA, hard deadline), place mode (tap shift+alt+z to arm, click a field, it lands there), monitor-correct toasts and a click-through target ring, RDP round 5 (persistent EVENT_SYSTEM_FOREGROUND hook + ctrl+shift+u manual unstick). 506 tests.
RENAMED 2026-08-22: folder is now D:\Devi\projectsctive\quiett and the GitHub repo is balupremkumar/quiett. Shortcuts, run.bat, the restart-app skill, vault wikilinks, PORTFOLIO row and the Claude memory dir all repointed. NOTE: the old empty voice-dictation folder could not be deleted because this session's shell was parked inside it; delete D:\Devi\projectsctiveoice-dictation at the start of the next session.

## Last session changes
2026-08-22 (shipped): built by five Sonnet agents on disjoint file sets, integrated and verified by the main thread. TWO REAL BUGS FOUND BY VERIFICATION, not by review: (1) the pre-flight probe ran BEFORE _force_foreground, so UIA read whatever was in front (often our own preview panel) instead of the target about to be typed into. Gate moved to after the foreground steal. (2) winfx HMONITOR restype defaulted to c_int, truncating the handle on 64-bit, which would have made every GetMonitorInfoW fail silently. Proof the confirmed path works came from a REAL dictation Balu made at 19:58:42 into Flightdeck while the build was running: park 77 chars -> type -> 'insert confirmed landed in the target' -> stash consumed, 19ms of verification. My scripted E2E harness could NOT verify that itself: a full-screen game (MortalShell2) held the foreground for the whole session, so nothing else could be focused, nothing landed, and UIA reported the game. Three harness checks stayed red for that reason alone. STILL UNVERIFIED, needs Balu at the keyboard: place-mode click-to-insert end to end, the armed overlay and target ring actually rendering (click-through, DPI, monitor placement), a toast landing on DISPLAY2, and anything RDP.
2026-08-22 (build): PLAN CORRECTED mid-build after chunk A. The plan's first draft escalated the failure UI on every UNCONFIRMED insert. Wrong: verification legitimately returns 'no signal' for terminals, RDP and every app with no accessibility layer, so that would have put a warning on most SUCCESSFUL inserts and trained Balu to ignore it. Corrected table now in PASTE_UX_PLAN.md section 5: confirmed-landed = restore clipboard + clear stash + silent; confirmed-failure or refused-by-preflight = recovery panel + error chime; no-signal = retain clipboard + keep stash armed + tray dot ONLY, no toast. Retention plus the dot is the silent safety net, loud is reserved for positive evidence of failure. Also ruled: the recovery panel's text stays READ-ONLY, because an editable box would have to steal focus, which defeats placing into a field you pick afterwards.
2026-08-22: diagnosis + plan only, no app code touched. Wrote PASTE_UX_PLAN.md from log/code evidence, not guesswork. Four defects named: D1 inject_text() returns INSERTED on SendInput acceptance alone and then _restore() wipes the clipboard, so a dictation into a non-editable focus is destroyed silently (the real cause of Balu's missed inserts); D2 toasts anchor to winfo_screenwidth/height = PRIMARY monitor only, so every failure notice lands on DISPLAY1 while he works on DISPLAY2; D3 no pre-insert target indicator; D4 the 4s post-hotkey RDP watcher logs 'RDP never took foreground' on every single release, i.e. it never fires. Balu ruled: place mode = arm-then-click (NOT hold-a-key, holding a modifier over RDP is the latching mechanism); client is mstsc; rename project folder AND GitHub repo voice-dictation -> quiett after the app work lands. UIA probe spiked and measured on this machine: comtypes already present (via pywebview), init 463ms one-off, GetFocusedElement+props 5-32ms. Chrome on a non-input pane returns the exact NOT_EDITABLE shape; a Tk Text returns the SAME shape despite being editable, which is why the confident negative is gated on known-good UIA providers and UNKNOWN always still attempts the insert.
2026-08-01 (later): P8 + P2 groundwork via agents, no app code touched. LICENSES.md + EULA-DRAFT.md written (commit 1cf6411): NO GPL/AGPL anywhere; pystray LGPLv3 needs a NOTICE entry; LM Studio redistribution blocker CLOSED with grep evidence (only two stale comments remain); pre-sale VERIFY: qwentts.cpp ships no upstream LICENSE files (engine MIT / weights Apache-2.0 claims trace to our own notes) — pull them from upstream before any paid release; leftover faster-whisper/ctranslate2 stack in venv should be excluded at PyInstaller time. brand/candidates/ (commit d9cac38): 3 icon directions (Fade/Comet/Capsule) + 2 wordmarks + preview.html for Balu's P2 pick. **Accent discrepancy surfaced: the kove.nz showcase runs VIOLET (Balu's artifact pick) but the shipped app is still cyan ion tokens and no ruling covers the app's colour — decide violet-unify vs keep-split at the icon pick.** Balu queue: icon/wordmark pick, accent ruling, EULA + LICENSES read.
2026-08-01: next-50 roadmap appended to BACKLOG.md (Q0-Q4 phases: Balu unblocks → productisation P2-P9 → reliability/accuracy → ruled-in feature guts → GTM), committed 18b0efa, pushed. Sequencing layer only, no code changed; PRODUCTION_PLAN P1 (naming) noted as DONE by the 2026-07-30 Quiett ruling — the plan doc predates it. config.json working-tree drift is the running app's live state (established pattern), left uncommitted.
2026-07-10: wrote PRODUCTION_PLAN.md — full productisation breakdown (UI polish per surface, brand/icon spec, PyInstaller+Inno installer, licence blockers incl. LM Studio redistribution, execution order P1-P11). Session hit usage limit right after; no code changed.
2026-07-10 (later): 4-agent UI/UX research sweep — 50-item premium revamp list landed in BACKLOG.md (dated heading). Root cause of jagged speech bubble confirmed: CreateRoundRectRgn hard mask (winfx.py:31-32) + no DPI awareness. No code changed.
2026-07-11: UI polish build (ROI rows 1-11) committed as 9c7d2b2 after Balu's OK. Batch 2 built via two Sonnet frontend agents and verified: dashboard (36 history search, 81 day headers, 64 geometry persist — save+restore tested E2E via window move, 77 About page, new VERSION "0.1.0" constant, confirm scheme with Balu) and popup/tray (15 DWM shadow, 11 badge status row, 22 tray left-click default, 23 grouped menu, 69 timed pause; 60 key chips found already done). App restarted clean (PID 14916), About + History verified by screenshot in dark theme. Committed d4194a4. Batch 3 done and verified: history (82 pin, 83 chips, 84 bulk select, 85 highlight, 87 retention) + popup (10 long-recording gate, 17 real gradient, 46 edge flash fallback-only, 53 countdown bar, 55 pin button, 57 WPM footer). history.py diff eyeballed (clean, additive), source="agent" tag added at main.py:464, app restarted clean (PID 18844), History chips/Select UI screenshot-verified dark theme. Committed 044b653. Batch 4 done: tray (25 ICO verified clean + --verify flag, 26 pulse already coherent, 68 recent-dictations submenu, 70 mic quick-picker) + popup (7 continuous waveform, 8 hover stop/cancel, 18 slide+fade entrance with animations:false kill-switch, 54 drag reposition per monitor). Committed 4bc1961. Batch 5 done (solo agent): 45 sound_volume + error chime, 47 motion policy incl. OS reduce-motion, 48 error states (+ real bug fix: failed mic start left is_recording() stuck), 49 empty states; copy pass stripped em dashes from all new user-visible strings, --warn token added to both dashboard palettes. App restarted clean (PID 24632), Settings (Animations toggle, Sound volume slider, status dot) screenshot-verified. config.json drift = dashboard_window/sound_volume live state, not a bug. Committed 8233a70, all pushed. Balu ruled: build ALL remaining items (waves B6-B11 planned in chat). B6 done+verified: 72 diagnostics (CORS privacy hole caught in review, transport reworked to pywebview bridge; API + mic probe tested E2E, page screenshot-verified light theme) + 51 streaming partials (cadence/backoff/tail-window/word-diff stabilisation; needs Balu's dictation). Real-use signal: Balu dictated twice at 14:27 on the batch 1-5 build, conf 1.0, injected into VS Code clean. Committed c122b4e, pushed. B7 done: popup (9 raw/cleaned toggle revived from dead vibe-mode code + 2 real bug fixes; 12 badge_animation styles; 13 closed n/a) + settings (30 reset, 76 import/export, 79 scale, 80 keyboard nav incl. dashboard half of 97, 19 silence slider, badge_animation select). App restarted clean (PID 21636). Visual click-through PENDING: Balu was fullscreen gaming during verify, screenshots suspended; compile + node checks clean. Balu clicked Test mic twice on Diagnostics at 14:46, probes clean. Committed f01371c, pushed. B8 done (35 consolidate — legacy Tk viewer removed, -242 lines; 38 replay via winsound; 39 MD/TXT/SRT/VTT export; 86 incognito + redact_patterns; 88 lazy waveform thumbs). App restarted clean (PID 4452); runtime click-through of play/export/incognito flows still pending (screenshots suspended, Balu gaming). Balu's 15:08/15:11 messages were live dictations, conf 1.0, on the B6/B7 build. Session handoff saved to memory resume.md (pre-limit save requested by Balu). Voice-profile recap given to Balu: recordings/ at 200-wav cap (rolling!), voiceprofile.py curator NEVER run, no TTS/narrate exists; offered profile rebuild + TTS roadmap, awaiting his call.
2026-07-10 (evening): light-mode rework, verified via dashboard screenshot. Fixed `def open` shadowing builtin in dashboard.py (broke ALL dashboard config reads/writes silently — renamed open_window, callers in main.py:974-977). Softened light palettes (dashboard CSS + preview.py _THEMES), fixed 6 hardcoded dark colours in preview.py, theme injected at dashboard launch (no dark flash). Hardened scripts/restart_app.ps1: relative-cmdline instances (stale 9:49am copy held mutex all day), pythonw3.13.exe children, CIM date crash.

2026-07-12: Balu reported "history not working, lots broken; menus frozen, no data". TWO root causes found and fixed. (1) Dashboard geometry persist (item 64) saved the minimized-window placeholder (-32000,-32000, 160x28) into config.json; reopen parked the window at the secondary-monitor corner at min size behind other windows. Fixed both sides in dashboard.py (_save skips minimized coords, _load rejects them → centered default); poisoned config entry removed. (2) THE BIG ONE: dashboard _HTML is a non-raw Python string; B7 added `val.join('\n')` / `split('\n')` (3 spots) — Python turned each \n into a literal newline inside a JS string literal, a syntax error that killed the ENTIRE dashboard script block: no nav, no data, all pages frozen. This shipped in f01371c because B7/B8 visual click-through was never done (Balu was gaming; the "node checks clean" claim was wrong). Fixed with \\n escapes; JS now node --check clean; History + Settings screenshot-verified rendering real data (PrintWindow PW_RENDERFULLCONTENT captures the page even when occluded — use scratchpad capture_dash.ps1 pattern). Also: removed the >30s long-recording confirm gate (item 10) per Balu — he dictates 1-2 min for real; gate+modal deleted from main.py/preview.py, always transcribe now. Also fixed dictation loss: 12:49 dictation (498 chars) was typed into the hidden pystray tray-icon window; inject.capture_foreground now returns 0 for own-app windows (own pid, or python process titled VoiceDictate = dashboard subprocess) and inject_text with no target copies to clipboard + toast instead of dropping. Tests: new tests/conftest.py stops pytest polluting the live app.log (MagicMock lines had landed in it); test_inject.py updated + 3 capture_foreground tests; 155 pass. App restarted clean (PID 24148). Committed 49211d3 (not pushed). Then wrote TTS_PLAN.md (speak-selection in cloned voice, P0-P5): P0 = engine spike Chatterbox vs Qwen3-TTS on the AMD RX 9070 XT (no CUDA — the key risk), needs ~1h of Balu's ear for similarity. Balu gave the go (feature must be OFF by default, zero idle resources — already the plan). P0 started: spikes/tts_p0/ sandbox (own 3.13 venv, spikes/ gitignored), bench_chatterbox.py written, chatterbox-tts pip install running in background. AMD path derisked by research: RX 9070 XT (gfx1201) officially supported on Windows ROCm 7.2, torch wheels at repo.radeon.com (maybe cp312-only → may need a side-by-side Python 3.12); Qwen3-TTS also has a community GGUF+Vulkan route (HaujetZhao/Qwen3-TTS-GGUF) matching the whisper.cpp stack. Sequence: CPU quality wavs first for Balu's ear, then ROCm latency leg, then Qwen3-TTS.
P0 results so far: PyTorch/ROCm path FAILS latency for both engines (ROCm 7.13 alpha on RDNA4 Windows is immature). Chatterbox: CPU rtf~5, GPU rtf~3.5 clean (9-10s per short sentence; setuptools<81 needed for perth pkg_resources; torchaudio 2.11 save needs soundfile not torchcodec). Qwen3-TTS 1.7B PyTorch: GPU rtf 13-38, unusable. Listening wavs for Balu ready in spikes/tts_p0/out/{chatterbox,qwen3tts}/. Route B WON: qwentts.cpp (C++ GGML port of Qwen3-TTS, MIT, Apache 2.0 weights) built with Vulkan (short-dir procedure per whispercpp-vulkan-build memory) — RTF 0.87, ~145ms to first audio warm, ~2.3GB VRAM. tts-server smoke-tested E2E: /v1/voices voice registration from sample_01 + /v1/audio/speech synthesis in the cloned voice; PCM streaming supported. P0 COMPLETE technically; full table + settled P1 shape in TTS_PLAN.md; memory tts-engine-qwentts-vulkan saved. Binaries in spikes/tts_p0/bin/, GGUFs in spikes/tts_p0/models/ (D:\qb build dir can be deleted). CRITICAL P1 constraint found: project profile.py shadows stdlib profile → any in-process torch import from project root breaks; moot if TTS stays subprocess-only (it should). Qwen ref_text for cloning obtained via running whisper-server (sample_01 transcript in spikes/tts_p0/ref.txt). App untouched all spike (still PID 24148 build, committed 49211d3); TTS remains non-existent in the product, nothing loads at runtime.

2026-07-12 (later): pushed 49211d3..90cd657, main in sync. Seeded NEW project D:\Dev\ai\projects\active\local-fitness-pal (voice-driven macro tracker, full design in its ARCHITECTURE.md, P0-P5 plan awaiting Balu's approval). Its P3 adds fitness.py + trigger phrases HERE (taskflow.py pattern, POST to 127.0.0.1:8091); handover file written. No voice-dictation code changed. config.json drift is live state (tts_enabled true for hands-on).

2026-07-13: LM Studio boot toggle shipped (driven from the fitness-pal session). Tray checkbox "Fitness Pal (LM Studio)" (top level, under Incognito), config key fitness_lmstudio_enabled (default false). Tick = lms server start + load qwen2.5-1.5b-instruct; untick = unload that model + server stop. New lmstudio_boot.py (daemon threads, CREATE_NO_WINDOW, toasts); boot-on-startup hook + hot-reload sync in main.py mirror the incognito pattern. 5 new tests, suite 178 green via `pytest tests -q` (bare pytest sweeps a broken vendored spike test under spikes/ — testpaths TODO). Verified live both ways via config hot-reload: boot ~10s, shutdown ~20s, VRAM freed. App restarted clean (PID 12080).

2026-07-14: stuck-Shift bug root-caused (bg job): it's the RDP session (L-64A858), not local — mstsc forwards hotkey modifier key-downs but drops key-ups; existing force-flush only ran on paste-into-RDP, so task capture (Ctrl+Shift+Alt), agent mode (Ctrl+Shift+C), cancel/too-short never flushed. Fix on branch worktree-rdp-stuck-modifiers (59122b8, pushed): inject.flush_hotkey_modifiers_async() wired into audio stop + cancel, RDP force-flush around TTS Ctrl+C grab. 161 tests pass. Merged into main 2026-07-14 (a34ee1e). Hardening committed adb6c45 + pushed (preview defers focus-steal via after() poll until keys physically up, 10s cap; inject paste/Enter/Ctrl+C abort while keys held, paste abort falls back to clipboard + toast; hotkey clears _held on pause). 178 tests pass, app restarted PID 9616. Only the in-RDP verify remains (Balu, remotely).

2026-07-25: read-aloud pace work (branch flightdeck/pi9oo2, NOT merged). Balu reported the voice speeding up through a long passage — reproduced against the live tts-server: one request for 12 identical sentences drifts 1.90s → 1.55s per sentence (-15%), while the same sentence in separate requests is 2.42s every time (sd 0.000). Per-sentence pace by request size: 1-2 sentences 2.40s, 3 = 2.16s, 6 = 1.87s. Fix in tts.py: `_chunk_text` splits input to `tts_max_chunk_chars` (120, 0 = off), producer thread synthesises chunk by chunk into a queue, `_SilenceGate` trims each render's ~0.4s lead-in and caps the tail at 0.35s with a 0.25s floor between sentences, 1.0s prebuffer on multi-chunk text. E2E re-verified: zero drift, exactly repeating segment pattern. New `tts_speed` (0.5-2.0, WSOLA pitch-preserving stretch in `_TimeStretcher`, RTF 0.008 after cutting the match window to 256; f0 stays 92 Hz at every speed) with a tray More → Read-aloud Speed radio submenu (0.7x-1.5x) writing config + `_on_set_tts_speed` in main.py. 198 tests pass. Real-time margin note: at 1.0x synthesis (RTF ~0.91 raw) is roughly break-even with playback, so very long passages may stretch the inter-sentence gaps slightly; below 1.0x there is plenty of headroom. Listening samples in the session scratchpad (sample_before.wav vs sample_after_{1,0.9,0.8}x.wav). App-level verify (tray submenu, hot reload) still pending: the app runs from the main clone, this is worktree code.

2026-07-25 (later): removed TaskFlow capture and agent command mode. Deleted taskflow.py, agent.py, llm_client.py, reformat.py, tests/test_taskflow.py. Stripped from main.py (task/agent sessions, trigger handling, readback, mark-done, auto-create, Ctrl+Shift+C hotkey, agent/taskflow config keys + validation + hot-reload), preview.py (task_mode panel, agent confirm modal, recording_task/reformatting badges, Agent + To-Do settings pages), tray.py (TaskFlow status, task counter, Agent Command Mode item), dashboard.py (TaskFlow + Agent settings sections, taskflow/agent history chips, diagnostics agent gate), api_server.py (POST /task). fitness.py now owns its own match_trigger (was imported from taskflow). health.py LM Studio check now gated on fitness_lmstudio_enabled, restart goes via lmstudio_boot. chime.play_task_added + chime.speak deleted. 110 tests pass, all modules import, dashboard JS node --check clean. Handover written to D:\Dev\ai\handovers\voice-dictation-to-to-do-app.md (only outside breakage: POST /task is gone). Study-mode narration plan written to STUDY_MODE_PLAN.md (P1 prosody engine, P2 toggle, P3 transport controls, P4 extras) — plan only, 4 open questions for Balu at the end.

2026-07-25 (final pass): study mode BUILT (STUDY_MODE_PLAN P1+P2) and the last de-bloat applied, both via parallel sub-agents. New narration.py owns segmentation, the pause table and per-segment rate for both modes (54 of its own tests); tts.py now synthesises and stretches segment by segment, with the stretch moved into the producer thread because rate varies per segment. Tray gained a Study Mode checkbox at top level and the speed picker now edits whichever mode is live (study_speed vs tts_speed, never both). Dashboard Settings gained a Read-aloud section: enable, speed, study mode, study speed, study pause length. Config keys study_mode (false), study_speed (0.95), study_pause_scale (1.0), all hot-reloaded and validated. Snippets and the repaste hotkey deleted (agent 2). One real bug caught by the narration agent and fixed: the final segment's stretcher flush was dropped, clipping the end of every stretched utterance. 147 tests pass. E2E against the live tts-server: normal 24.0s vs study 29.3s for the same passage, study planned 11 segments with heading 1.00s pause at 0.90x, comma 0.15s, sentence 0.45s, paragraph 0.90s, list items 0.55s, and the measured gaps track the plan.

2026-07-25 (later still): Fitness Pal divert removed too, on Balu's call. Deleted fitness.py, lmstudio_boot.py and their tests; stripped the food-log branch, fitness_* and lmstudio_model config, the tray "Fitness Pal (LM Studio)" toggle, and the whole LM Studio half of health.py and the dashboard status dot. LM Studio is now unused by this app. 93 tests pass. Handover written to D:\Dev\ai\handovers\voice-dictation-to-local-fitness-pal.md.

2026-07-27: bug-fix + accuracy session. (1) Dashboard is now genuinely single-instance: it binds a loopback control port (8093, `dashboard_control_port`) and the bind is the lock, so a window left over from an earlier main.py run is raised and re-pointed instead of a second one opening. open_window sends {show,page}; a leftover _proc guard still covers the boot window. (2) History live-refreshes: main.py fires dashboard.notify_change("history") after every save, the page reloads in place keeping scroll/search/selection, with a 4s get_history_stamp poll as the backstop. Deferred while bulk-select is open. (3) History rows are addressed by timestamp, not index — a dictation landing while the page is open used to shift every index by one, so delete/pin/play hit the neighbouring entry. history.resolve_index + a cross-process file lock (history.transaction) now guard the read-modify-write the dashboard and main app both do. (4) Lost insert target fixed: the foreground is captured at hotkey-down as well as hotkey-up, and used when our own badge/panel holds focus at release (app.log 18:46:17 was a real case). (5) Panel latency: the WAV write and history save moved off the critical path into a thread, after preview.show. E2E verified with a live dashboard: second launch exits in 432ms and re-points the first window (screenshot), push refresh and poll refresh both land, minimised window restores to foreground.
Accuracy pass over the real corpus (100 dictations, 200 recordings): several "Whisper errors" were our own post-processing. Fixed in transcribe.py — dots and commas inside a token no longer get split ("make.powerapps.com", "kove.nz", "anime.js", "2,600"), bare "one" stays a word (it digitised six times and read as a typo), hyphen-split words rejoin ("front -end"), speaker-turn dashes dropped, non-speech tags and whole-transcript hallucinations ("*sad music*", "Thank you.") dropped, and a decoder repetition loop (one dictation repeated the same sentence 12 times) collapses. New _rejoin_vocabulary_splits repairs vocabulary terms Whisper breaks mid-word ("Share Point", "MyF itnessPal", "Wai kato", "Cop ilot") and canonicalises their casing, driven by custom_vocabulary rather than a growing corrections list. config.json: initial_prompt set (was empty — this alone stopped the repetition loop on re-run), +23 vocabulary terms, +11 corrections (crisis branch → Christchurch branch, NTN testing → end-to-end testing, kobe.nz/cove.nz → kove.nz), and "like" removed from filler_words — it was deleting real words ("looks like the screenshots" → "looks the screenshots"). Replaying the new pipeline over all 100 stored transcripts improves 21 and regresses none. 194 tests pass.
BACKLOG.md: 100 new items (101-200) across accuracy, speed, insert reliability, dashboard, panel, read-aloud and packaging.

2026-07-30 (later): SHOWCASE SHIPPED to kove.nz, app untouched all session (still PID 27336 build). Concept UI (kove-site\voicedictate-demo\app.html: "ion glass" dark identity, 8 pages incl. Cloud AI + Meeting Mode roadmap pages, simulated dictation sequence) + 57s deterministic reel (reel.html/reel.js, flightdeck ?at= engine). Balu's mid-session ruling: no generic AI-dashboard furniture (green status pills, stat-tile grids, sparkline-in-a-card); v2 identity = giant ultralight numeral, hairline rhythm bars, glass planes on aurora, "0 BYTES OUT" seal. Design brief: SHOWCASE_DESIGN.md. Live: kove.nz/voicedictate-demo/app + /reel, linked from the refreshed kove.nz/work-voice-dictation (stale command-mode/LM-Studio claims removed). This showcase is the design target for the real app's P4 dashboard pass later.

2026-07-30: productisation strategy session. 3-agent research sweep (competitors, feature gaps, pricing) → dossiers in D:\Dev\ai\research\ (voice-dictation-competitors-2026.md, voice-dictation-feature-gaps-2026\DOSSIER.md, voice-dictation-pricing-2026.md). PRODUCT_STRATEGY.md written: positioning = fully-offline + premium polish (unclaimed), dictation + cloned-voice study read-aloud combo has no competitor. Balu ruled (brain/rulings.md 2026-07-30): name stays VoiceDictate for now (glyph-only brand work), pricing deferred to pre-P10, local LLM cleanup pass APPROVED (narrow reopen, off by default, TTS subprocess pattern), all 4 showcase UIs approved (meeting mode, cloud AI page, voice edit commands, multilingual). No code changed.

2026-07-30 (night): QUIETT UI BUILD SHIPPED, all 7 phases, via 2 parallel agents + orchestrator integration. theme.py is the single token source (dashboard CSS + Tk flats + tray). Dashboard: ion-glass shell, Home page (real stats: get_home_stats), Voice page (real narration pause plan, /speak sample button), Dictionary (learn_from_edits toggle + recent_promotions feed), Diagnostics (netstat socket probe via GET /diag/sockets, "external connections: 0" live), voiceline status strip, seal. Tk panel/badge on theme tokens + apply_backdrop acrylic behind panel_acrylic key (transparentcolor route NOT attempted, orchestrator to eyeball; BACKLOG 20 still deferred). New mic-to-caret icon set (hand-tuned 16/20px). 235 tests green. Integration bugs found+fixed: applyTheme wrote to removed themeIcon (killed entire JS boot, INIT ERR instrumentation found it), boot now guards bridge-ready ordering, .iby.pin CSS, learn_from_edits autosave default.
INCIDENT 2026-07-30: my API-enumeration mock script called DashboardAPI.clear_history() and WIPED history.json (100 entries). Recovered by re-transcribing all 200 recordings/ WAVs through the live pipeline (timestamps from filenames, source="recovered"); user-edited final text and target-app metadata are lost, machine transcripts stand in. clear_history now requires confirm=True so no argless call can ever do that again. No app-side history backup exists; consider one (BACKLOG).
Balu hands-on pending: real dictation on the new panel (acrylic look + ion waveform), Play sample on Voice page (speaks aloud), tray icon eyeball at 16px, light theme sweep.

2026-07-30 (evening): QUIETT_UI_PLAN.md written — the build guide for porting the showcase "ion glass" design into the real app, local-only scope (no Cloud AI, no Meeting Mode, no installer work). P1 shared theme.py tokens → P2 dashboard shell → P3 Home/stats page → P4 panel acrylic (BACKLOG 14) → P5 icon set (unblocks PRODUCTION_PLAN P2) → P6 Voice page + auto-learn dictionary (BACKLOG 41, only new backend logic) → P7 diagnostics socket-listing probe. Plan only, no code changed; awaiting Balu's go.

2026-08-04: three Balu-reported fixes built via delegated agents (Haiku scouts + Sonnet builders, Fable planned only). (1) Insert-failure recovery: inject_text/inject_text_and_submit now return "inserted"/"clipboard"/"failed" on every branch, UAC branch no longer loses text (copies to clipboard first), failed inserts raise a 15s toast with "Insert again" resolving the target at click time, toasts got WS_EX_NOACTIVATE (winfx.apply_no_activate) so the click doesn't steal focus, new tray item "Insert Last Dictation", helper insert_text_now/_resolve_paste_target in main.py:266-327. BACKLOG 139 ticked. (2) Dashboard speed: hide-on-close keeps the subprocess resident (warm show 39-61ms vs cold ~755ms standalone), dashboard_prewarm config key (default true) boots it hidden ~6s after start (first show 37ms), hist.purge off the launch path, JS boot loads the requested page first then settings/about/stamp concurrently, network probe one deferred run for the seal + poll only while Diagnostics visible, atexit + control "quit" teardown (no orphaned WebView2), perf log lines in app.log. (3) Branding: desktop Quiett.lnk created (main clone target, real icon), stale Voice Dictation.lnk deleted, quiett-* export filenames, logger/chime/conftest/run.bat/restart_app.ps1 renames, create_shortcut.ps1 prefers assets\icon.ico. node --check clean, 278 tests pass. Autosave-after-init-reorder needs one manual Settings click post-restart (harness limit, not a regression).

2026-08-12 (crash recovery): previous session crashed after the RDP round 2-4 commits; recovery session confirmed no code lost (app live on the round-4 build, dictations in app.log), committed the stranded STATE/BACKLOG dashboard edits as 3b5a7a2, refreshed memory resume.md. Unpushed: 6428110..3b5a7a2 (5f5d223, d716efe, 3b5a7a2) — awaiting Balu's push call. config.json drift left as live state (carries the rdp_clipboard_* keys). .serena/ = untracked Serena cache, ignored.

## Next steps
- [ ] Build PASTE_UX_PLAN.md chunks via Sonnet agents: A clipboard retention + stash.py + tray state (P1), B monitor-correct toasts + escalation panel (P1), C place mode ctrl+alt+v arm-then-click (P1), D targetprobe.py + target ring + post-verify (P2), E RDP round 5 foreground WinEvent hook + unstick hotkey (P2). A and B first and independent; C needs A; D after B; E independent.
- [ ] Acceptance test for the whole plan: dictate into Chrome with NO text field focused. Today that destroys the text silently. Must become recoverable in one action.
- [ ] After the build lands and pushes: rename D:\Devi\projectsctiveoice-dictation -> quiett and the GitHub repo, then repoint Desktop/Start Menu Quiett.lnk (both target launch.vbs by absolute path), the Claude project dir, and vault wikilinks.
- [ ] NOTE for the D chunk: the target-app name label already exists on the panel (item 58, built 2026-07-10 night). The RING is the new part, not the label.
- [x] Pushed 2026-08-12: 6428110..cc972a0 (RDP rounds 3-4 + dashboards + crash-recovery notes), origin in sync, session closed clean.
- [x] 2026-07-27: committed b616388, merged to main as f553d83, app restarted on it (PID 27336). NOT pushed to origin — ask Balu.
- [ ] Balu's hands-on pass on the 2026-07-27 build: dictate once and watch the panel appear (should be quicker), dictate with the dashboard open on History (the row should appear without touching the window), tray → View History twice (one window, raised both times), and a dictation where our own panel still has focus at release (should insert, not fall back to the clipboard).
- [ ] Five ambiguous mistranscriptions are listed under Open bugs waiting on Balu's ear before they become corrections.
- [x] 2026-07-25: merged to main, committed 7446f43 + 65edfd6, pushed (4a64704..65edfd6), app restarted on it (PID 11356).
- [ ] Balu's hands-on pass on the new build: tray Study Mode toggle, More → Read-aloud Speed (it edits whichever mode is live), dashboard Settings → Read-aloud section renders, and a real Ctrl+Shift+S over a study passage in both modes.
- [ ] Balu to answer the 3 questions still open at the end of STUDY_MODE_PLAN.md before P3 (transport controls) is considered.
- [ ] LATER, when Balu asks (he builds the app first): fitness.py + trigger phrases ("food log" etc.) posting raw transcripts to local-fitness-pal on 127.0.0.1:8091 — taskflow.py pattern, spec in that project's ARCHITECTURE.md §4. Voice-dictation stays otherwise uninvolved in that project.
- [ ] Balu hands-on checks, batches 2-4: dictate once (waveform, badge status row, hover stop/cancel, slide+fade, WPM footer, countdown bar + pin), drag the panel header and reopen, tray right-click (grouped menu, Recent Dictations, Pause submenu, More → Microphone), tray left-click opens dashboard, history chips/pin/bulk select, >30s recording confirm.
- [x] Ruled 2026-07-11: drag wins over fixed position; VERSION stays 0.1.0; Win11-only; pin emoji stays; popup stack decision waits for Balu's real-use test.
- [x] Double-enter insert: Balu reversed the bug ruling same day (2026-07-11) — by design; second Enter inserts into the focused field. No code change was made. Verify it still behaves that way during the hands-on pass.
- [x] Naming DECIDED 2026-07-30: **QUIETT** (rulings.md; NAMING.md round 3 has the full verdict trail — Balu's first pick Hush failed his no-collisions gate, direct same-category products exist). Renamed VoiceDictate→Quietype→Quiett same day across app code (sed sweeps, 194 tests pass, restarted PID 7392, API reports service=Quiett; config vocab keeps VoiceDictate and now has Quiett) and kove.nz (kove.nz/quiett-demo/app + /reel live, work page says "a product called Quiett"). Both repos committed AND pushed (app cc66433, site bdcace9). Stale worktree .claude\worktrees\rdp-stuck-modifiers still says VoiceDictate, untouched on purpose. P2 icon + BACKLOG 50 accent unblocked. Domains to check at purchase: quiett.app / quiett.nz; IPONZ before any sale.
- [ ] Newly in scope: BACKLOG 51 (streaming transcript, H/L) and 72 (diagnostics page, H/M).
- [x] Pushed to origin 2026-07-11 (67b70d6..8233a70), main in sync.
- [ ] BACKLOG item 20 (popup stack decision) now also owns the deferred ULW badge; only matters if product targets Win10 or wants soft shadows.
- [ ] PRODUCTION_PLAN.md P2 (desktop icon design) — pipeline exists now (make_icons.py); P1 product name still needs Balu.
- [ ] Verify the sticky modifier fix survives a real remote-desktop session.
- [ ] TTS: Balu ratified the voice (vulkan_01/04 closest), ruling in brain/rulings.md. P1+P2 SHIPPED as 7faeff1: tts.py (lazy tts-server subprocess, idle reaper, ICL voice registration, PCM streaming to sounddevice, tts_reference override), Ctrl+Shift+S speak-selection (NOT ctrl+alt+<x> — record-hold conflict), get_selected_text via GetClipboardSequenceNumber. E2E verified: hotkey fires (log 14:55), grab returns real selection, real server speak 3.4s warm, clean shutdown. tts_enabled left TRUE in Balu's config for his hands-on (default in code is false; zero cost while idle). Ref sweep done; Balu ruled the original vulkan_01 render beats all 10 sweep renders. Its exact clip+transcript pinned as voice_profile/tts_reference.{wav,txt}, config tts_reference set, sidecar-transcript support + seed 42 in tts.py, voiceprofile.rebuild() protects the pinned pair. Committed 90cd657, app restarted PID 24920, 161 tests. Registration verified: "voice registered from tts_reference.wav (ICL mode)". Next: P3 UI (tray toggle, Settings section, dashboard Voice page — design suite), P4 extras, P5 licence/installer.
- [x] Double-enter insert confirmed intended by Balu 2026-07-11.

## Open bugs
HOTKEY DEFAULTS IN PASTE_UX_PLAN WERE BROKEN (caught 2026-08-22 by the chunk E agent, fixed in integration): the plan picked place_hotkey=ctrl+alt+v and unstick_hotkey=ctrl+alt+shift+u, but ctrl+alt IS the record hold and hotkey._on_key only tests that every mod set is held, so extra keys are ignored and BOTH combos would start a recording before doing their job. New defaults: place=shift+alt+z (Wispr Flow precedent for the same function, rare enough not to steal from anything; plain alt+shift is the Windows layout switch but a letter suffix does not trigger it), unstick=ctrl+shift+u. Never pick a ctrl+alt+<x> hotkey in this app; config.json's tts_hotkey comment already said so.
SILENT INSERT LOSS (D1, open): inject.py:1068 reports INSERTED whenever SendInput accepts the events; nothing verifies the text landed. The _restore() thread at inject.py:1221 then puts the old clipboard back 150-450ms later, so a dictation aimed at a non-editable focus is gone entirely, with no toast. Fix = chunk A.
TOASTS ON THE WRONG MONITOR (D2, open): preview.py:609-611 and _show_edge_flash use winfo_screenwidth/winfo_screenheight, which is the primary display under Tk on Windows. config.json has panel_position for DISPLAY1 and DISPLAY2. Every failure notice has been invisible whenever Balu is on the second screen. Fix = chunk B.
Ambiguous mistranscriptions left uncorrected on purpose, need Balu's ear: "the SAP will be tested" (probably "the app", but SAP is a real product he may mean), "you're going to get some wet errors" (probably "red errors"), "the X-Men Eyes models that we have" (context is animation models), "Heesh" as a whole utterance, "bench, rear squat".
The vocabulary rejoin will capitalise an ordinary "share point" into "SharePoint". Accepted trade: he never uses the phrase literally.
Sticky Ctrl/Shift/Alt in RDP — round 2 fix committed 2026-08-12 (6428110, plan in RDP_FIX_PLAN.md): the July fix had a race (flush thread vs preview focus steal, flush silently skipped), no retry when RDP regained focus, unflushed Ctrl+Shift+S/cancel exits (the stuck-Shift source), and an rdpclip clipboard race (Ctrl+V 30ms after SetClipboardData + 450ms restore = stale/no paste). Now: 4s foreground watcher, pre-focus-steal flush in preview, TTS + panel-cancel flush wiring, rdp_clipboard_settle_ms 250 / rdp_clipboard_restore_delay_ms 3000, msrdcw.exe matched, skips logged. Round 3 (5f5d223, Shift/Ctrl down+up taps in the force flush) made it MUCH WORSE — Balu: remote Ctrl latched almost constantly, only killing the local app + mashing modifiers freed it. Root cause of the worsening: taps injected synthetic DOWNS into a channel (mstsc forwarding around focus changes) known to drop UPS, and the panel's focus steal (SetForegroundWindow + AttachThreadInput on mstsc's queue) fired ms after the taps — our own flush latched the remote every dictation. Round 4 (d716efe, Fable-implemented): taps REVERTED (force flush = key-ups ONLY, regression-tested); panel NO LONGER steals focus when an RDP window is foreground (flushes ups then leaves focus with mstsc; suppressed global Enter/Insert/Esc hooks commit/cancel, click to edit); _force_foreground no-ops when target already foreground (no AttachThreadInput at insert); force flush logs "SendInput inserted n/8" + local logical modifier snapshot (discriminates local vs mstsc-side corruption on the next report). App restarted PID 7752, 289 tests. UX change to tell Balu: panel over RDP no longer takes focus — Enter/Esc still work, click the panel to edit text. In-RDP verify unchanged; if latch recurs, pull the n/8 + "local logical mods" lines from app.log. ROUND 5 EVIDENCE 2026-08-22: latch still reported (scrolling in RDP zooms = Ctrl held remotely), and every hotkey release in app.log logs "RDP never took foreground within 4s, no flush needed" — the round-2 watcher provably never fires. Client confirmed mstsc, which _is_rdp already matches, so the miss is the WINDOW not the detection. Plan: persistent EVENT_SYSTEM_FOREGROUND hook flushing key-ups whenever an RDP window gains focus, a manual ctrl+alt+shift+u unstick hotkey that works regardless of detection, and the log line gains the foreground exe/class/title so the next occurrence identifies itself.
Titlebar follows Windows theme, not app theme (light app + dark OS = dark titlebar); small DWM call if wanted.
Theme switch still needs app restart for Tk surfaces (BACKLOG item 4).
2026-07-10 changes committed in 4 commits, 3aa16a0..b033298 (light-mode fixes, restart script, config theme=dark via fixed save path, plan/backlog docs incl. items 51-100); push to origin not done, ask Balu.
Top-25 ROI ranking given in chat 2026-07-10 evening: quick wins first = DPI awareness, DWM corner pref, target-app indicator (58), instant-apply, autostart, titlebar sync (63).
2026-07-10 (night): built UI_POLISH_PLAN.md items (ROI rows 1-11, Balu-approved scope): DPI awareness v2, DWM rounded corners (fixed the jagged bubble — verified 3x zoom), titlebar theme sync, instant-apply settings (Save button gone), autostart toggle, target-app "→ VS Code" indicator + status dot on panel, live theme sync incl. theme:"system", monochrome theme-aware tray icon + scripts/make_icons.py → assets/. M5 ULW badge descoped (DWM already fixed edges; see plan + BACKLOG 1/20). App restarted + verified per milestone. Committed 2026-07-11 as 9c7d2b2.

## Key files
main.py (wiring), hotkey.py, inject.py, preview.py, agent.py, llm_client.py, taskflow.py, api_server.py, config.json.
Launch: run.bat / launch.vbs; log: app.log.
